{
  "name": "carbon-coin-web",
  "version": "0.1.0",
  "private": true,
  "description": "Static, frontend-only Astro site for the Carbon Coin MPToken demo, signed via GhostSig (ghostsig.dev) in the browser -- no server, no secrets.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro check"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "qrcode": "^1.5.4",
    "xrpl": "^4.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.7.2"
  }
}
